# Environments
.venv/
__pycache__/
*.pyc
.env



# Data (never commit raw or derived data)
data/raw/
data/interim/
data/processed/

# Trained models / artifacts
models/

# Logs (generated at runtime)
results/logs/

# Trained models / artifacts (root-level output dir only — NOT src/models/,
# which is source code and must be tracked)
/models/
/netevolve.egg-info/
RoNeTC_Implementation_Plan.md