# data/raw/

Original, unmodified dataset files. **Never edit or overwrite files in this folder by hand or in code.**

Expected files (place them here manually — see main README's "Dataset Setup" section):

```
data/raw/UNSW_NB15_training-set.csv
data/raw/UNSW_NB15_testing-set.csv
```

This folder is excluded from Git via `.gitignore`.
