# data/interim/

Temporary, intermediate data produced while cleaning/filtering the raw dataset
(e.g. after class filtering, before final feature processing). Safe to delete
and regenerate at any time by re-running `scripts/run_preprocessing.py`.

This folder is excluded from Git via `.gitignore`.
