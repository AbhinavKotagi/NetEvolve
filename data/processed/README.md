# data/processed/

Final, machine-learning-ready arrays and metadata (e.g. `X_train.npy`,
`y_train.npy`, `processed_metadata.json`). Produced by
`scripts/run_preprocessing.py`. Safe to delete and regenerate.

This folder is excluded from Git via `.gitignore`.
